#
# Rofi hotkeys
# Any shortcut must be using super + <<key>>
#

# Rofi launcher
super + {space, p}
	rofi -show {drun, power-menu -modi power-menu:rofi-power-menu}

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd && dunstify "sxhkd was reloaded"

#
# Program launcher hotkeys
# Any shortcut must be using super + alt + <<character>>
#

# Open Kitty Terminal
super + alt + t
	kitty

# Open librewolf
super + alt + b
	librewolf

# Open obsidian
super + alt + o
	obsidian

#
# System or System like hotkeys
# Any command must be using super + <<character>>
#

# Open file manager
super + e
	nohup nautilus ~/Downloads/ > /dev/null 2>&1 &

# Close focused windows
super + w
	bspc node -c


# 
# bspwm hotkeys
# Any key must be using super + <<character>> 
#

# Alternate between the tiled and monocle layout
super + m
	bspc desktop -l next

# swap the current node and the biggest node
super + g
	bspc node -s biggest.local

# Set the window state
super + {t,s,f}
	bspc node -t {tiled,floating,fullscreen}

# FOCUS/SWAP
# focus the node in the given direction
super + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,north,south,east}


# focus the next/previous node in the current desktop
super + {_,shift + }n
	bspc node -f {next,prev}.local

# focus the last node/desktop
alt + Tab
	bspc node -f last

alt + shift + Tab
	bspc desktop -f last

# PRESELECT

# preselect the direction
super + ctrl + {h,j,k,l}
	bspc node -p {west,north,south,east}

# cancel the preselection for the focused node
super + ctrl + space
	bspc node -p cancel

#
# MOVE/RESIZE
# Any window move must be using ctrl + alt + <<character>>
# 

# expand a window by moving one of its side outward
ctrl + alt + {h,j,k,l}
	bspc node -z {left -20 0,top 0 -20,bottom 0 20,right 20 0}

# contract a window by moving one of its side inward
ctrl + alt + shift + {h,j,k,l}
	bspc node -z {right -20 0,bottom 0 -20,top 0 20,left 20 0}

# move a floating window
ctrl + alt + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}

# Move window testing
super + alt + {Left,Down,Up,Right}
	dir={west,south,north,east}; \
	bspc node -s "$dir.local" --follow \
	 || bspc node -m "$dir" --follow

#
# Audio hotkeys
# Keyboard: Logitech MX G915 TKL
# External program: playerctl
# Installation: apt install playerctl
#

# Media Control
XF86AudioPlay
	playerctl play-pause

XF86AudioNext
	playerctl next

XF86AudioPrev
	playerctl prev

XF86AudioMute
	amixer -q sset Master toggle &

XF86Audio{Lower,Raise}Volume
	amixer -q sset Master 5%{-,+} unmute &
